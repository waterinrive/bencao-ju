# 本草·局 — 黑客松 Demo（10 天可执行书）

> 本文件 = demo 简化版 + 升级到长期版（`DESIGN.md`）的路径 + 排期 + 技术栈 + 风险。
> 长期版完整需求见 `DESIGN.md`（锁版，不再动）。

## 目标 & 评委 wow 点
- **目标**：10 天做出可玩网页 demo，撑评委"新意 + 能跑"。
- **三个 wow 点**：① AI 从知乎搜索真实病证/医案生成 BOSS 关卡（每日 6 点刷新）② 君臣佐使四角色位出牌（核心教学机制）③ 麻黄汤完整一局对战（体验"缺哪个痛哪个"）。

## 范围（从 DESIGN.md carve）
**做**：固定麻黄汤教学关 + 今日病证 BOSS（知乎搜索真实医案→AI 提取病机）+ 四角色位结算 + 内联提示/失败因果 + 太医院 Agent。
**不做（长期项，留升级路径）**：八法全流派、升降浮沉、十八反全禁忌名单、Roguelike 跨关成长、知乎搜索补长尾卡牌、多轮 Agent、气血津液多维状态、严格相杀减毒。

## 技术栈（已锁·按官方 skill 修正）
- 前端：React + TypeScript + Vite。点选指派（不拖拽、无 DnD 库）。图标 emoji/SVG+彩点占位。纯 CSS（不引 Tailwind）。useReducer（无 Redux）。
- 战斗引擎：纯 TS `src/shared/combat.ts`，前端直接调，对战不走后端。留 `assert` 自检。
- 后端：Node + TypeScript + Hono（极小）。两个带密钥的路由：`GET /api/today-boss`（返缓存 BOSS+生成日志）、`POST /api/ask-mentor`（太医院点播，本地规则兜底）。Access Secret 只在后端 .env，不进前端/日志/仓库。
- **AI 全走知乎官方 HTTP API**（不自建 MCP、不引外部 LLM）：
  - 知乎搜索：`GET https://developer.zhihu.com/api/v1/content/zhihu_search?Query=...&Count=10`（Bearer+X-Request-Timestamp秒级）→ Items{Title,ContentText,Url,AuthorName,VoteUpCount}。按证型主题池（风寒/风热/湿热/气虚/阴虚/血瘀/肝郁/痰湿/实热/阳虚 医案）day-of-year 轮转，每天一种。
  - 直答（=LLM，OpenAI 兼容）：`POST https://developer.zhihu.com/v1/chat/completions`，model `zhida-fast-1p5`（boss 生成 + 太医院，快且稳；thinking-1p5 网关 504，不用）。
  - 搜索（补卡牌典故，可选）：`/api/v1/content/global_search`。
  - 额度：搜索 5000/天、直答 100/天、热榜 100/天。搜索驱动 + 预生成缓存下极充足。
- boss 生成：**预生成+缓存**。脚本 `scripts/gen-today-boss.ts`（或 cron/regen 路由）跑一次 → 写 `data/today-boss.json` + 生成日志。前端读缓存，演示不翻车。生成日志做"这关怎么来的"展示面板保 wow 感。
- **每日更新机制（与 MCP 无关）**：核心是 `generateBoss()` 函数——知乎搜索真实病证医案（证型主题池 day-of-year 轮转）→ 直答(zhida-fast) 带schema约束**提取病机（不做隐喻）** → 校验clamp → JSON。调用方三选一，逻辑不变：① cron 定时跑 `0 6 * * *`（每日 6 点刷新主力）② `POST /api/regen-boss` 手动重生成（dev/演示）③（远期可选）薄包一层 MCP 让 agent 交互触发。砍掉 MCP 预封装不影响每日更新，因为每日更新靠 cron 跑同一个函数。
- 数据：`data/` 静态 TS/JSON，不引数据库。
- 仓库：单包 pnpm。`src/{web,server,shared}` + `scripts/` + `data/`。Node 20+。
- `pnpm dev` 起前后端，`pnpm gen:boss` 预生成，`pnpm test` 跑结算自检。
- **部署**：必须公网可访问（提交要求）。单 Hono 服务同时托管前端静态+API，部署到 Railway/Render/Fly 之一（待定）。

---

## 各系统 demo 版 + 升级路径

### BOSS / 病证
- **demo**：① 固定教学关=风寒表实证（麻黄汤）。② 今日病证关=AI 从知乎搜索真实医案提取的 1 个 BOSS。schema 完全同长期版（4.3）。兼证 0–2、意图 4 种。
- **升级到长期**：八纲全标签、八法全流派、BOSS 多阶段/多证型切换、动态生成 + 难度自适应 + 战斗模拟平衡测试、全球每日排行。

### 卡牌 / 中药
- **demo**：第一层全做（药名/主功效/四气/归经/药力）+ 第二层的"毒性"和"七情配伍标签"必做（教学闭环"缺佐痛"靠毒性、羁绊演示靠七情）。副功效、五味看工期。
- **升级到长期**：第二层全开 + 第三层（升降浮沉/剂量炮制/道地药材）；知乎搜索补长尾卡牌典故。

### 君臣佐使四角色位
- **demo**：四角色位全做，"缺哪个痛哪个"反馈全做。
- **升级到长期**：角色弹性进阶（同卡不同角色不同效果）、反佐子机制（与君相反药性防格拒）。

### 配伍七情
- **demo**：相须（×1.5）、相恶（×0.7）、相反（毒伤/禁）做。相使、相杀看工期。
- **升级到长期**：七情全 + 十八反十九畏全名单（硬禁忌）、五味羁绊（辛甘化阳等）、相使/相杀严格化。

### 引经药
- **demo**：使药归经命中折扣做（命中×1.0/不中×0.6/缺使×0.7）。种子库只放麻黄汤关需要的归经（肺/膀胱）。
- **升级到长期**：十二经引经药全表 + 部位引经（桔梗上行/牛膝下行等）+ 升降浮沉方向性。

### 战斗结算
- **demo**：9.1 管线 + 9.2 规则全做。**佐药减毒用宽松版**：有佐（非空）即反伤减半，不要求严格相杀关系。理由：demo 药池小，严格相杀易卡死；宽松版保住"缺佐反伤"教学反馈即可。
- **升级到长期**：严格相杀（佐必须与君有真实相杀/相畏关系才减毒）+ 反佐防格拒。

### 玩家 HP / 胜负 / 抽牌
- **demo**：玩家 HP=20（单血条）。BOSS 每回合按意图扣血。BOSS HP 归零=胜，玩家 HP 归零=败，8 回合上限超时=败。一局 3–5 分钟。**三层牌堆 + 心力费用**：抽牌堆/弃牌堆/消耗堆；每回合弃手牌→补到 5（抽空洗弃牌堆回）；出牌扣心力（平和0/峻烈2/大毒3，base3/回合），心力不足则不能出；峻毒药（附子）出过进消耗堆不回流。麻黄汤 cost 2+1+1+0=4 > 3 → 一回合出不完，逼跨回合削血。牌池：教学关=麻黄汤4+通用干扰；今日BOSS关=通用池19。评委展示模式可固定手牌。
- **升级到长期**：气血津液多维状态、无回合上限改"病人状态恶化进度条"、Roguelike 跨关成长、卡牌炮制升级、地图事件获取药材。

### 新手体验
- **demo**：第一层内联提示 + 第二层失败因果（规则化，跟结算逻辑一起做，零额外成本）+ 第三层太医院 Agent（`ask_mentor` MCP tool，带局况，接知乎直答，本地规则化兜底）。Agent 只给规则方向不点名具体药。
- **升级到长期**：多轮对话、教学梯度（新手/进阶差异化提示）、可调提示强度、接知乎答主语料持续陪练。

### AI 侧（知乎开放平台）
- **demo**：知乎搜索病证→BOSS（后端调 `zhihu_search` 证型主题池轮转 + 直答 `zhida-fast` 提取病机，预生成缓存 + cron 6点刷新）；太医院（直答 `zhida-fast` 带局况上下文，本地规则兜底）。全走知乎官方 HTTP API，Access Secret 仅后端。
- **升级到长期**：付费额度/官方合作、知乎搜索补长尾卡牌、直答多轮对话、病机库扩至数百证型、按季节/时事出应景 BOSS。

### 病证搜索主题池
- **demo**：10 个证型主题 query（风寒/风热/湿热/气虚/阴虚/血瘀/肝郁/痰湿/实热/阳虚 医案 辨证），day-of-year 轮转，每天一种，覆盖八纲常见证。AI 从搜索返回的真实医案 ContentText 提取病机做 BOSS，不编造隐喻。
- **升级到长期**：知乎直答动态生成 + 证型分类器 + 数百证型 + 按节气/时令出应景 BOSS。

---

## 排期（10 天）
| 天 | 任务 |
|---|---|
| D1–2 | 项目骨架 + Zhihu HTTP 客户端（Bearer+timestamp）+ BOSS/药卡 schema + 麻黄汤 4 味药种子 + 风寒表实证 BOSS 数据 |
| D3–5 | 前端对战页 + 四角色位点选 + 结算管线 + 内联提示 + 失败因果文案 |
| D6–7 | boss 生成脚本（知乎搜索病证→直答→schema 校验→today-boss.json）+ `/api/today-boss` + `/api/regen-boss` + 太医院 `/api/ask-mentor`（直答+本地兜底）+ cron 6点 |
| D8 | "今日病证 BOSS"链路打通 + 前端渲染 + 生成日志展示面板 + 难度校验 |
| D9 | UI/文案/教学引导打磨 + 部署上线（Railway/Render） |
| D10 | 录演示视频（兜底）+ 缓冲 + 提交材料整理 |

## 风险 / 兜底
- **知乎额度**（搜索 5000/天、直答 100/天）→ 直答本地规则兜底 + 预缓存。
- **AI 生成 BOSS 变态组合** → 难度校验器（hp + yitu.value×5 + 兼证数×5 ≤ 75）+ 病机一致性闸（清热泻火/辛凉解表→热证、温里散寒/发汗解表→寒证，矛盾即拦截）+ 19 味通用解药池覆盖全 12 功效 + 演示用缓存结果保稳。
- **现场演示翻车** → 预缓存一个"今日 BOSS"+ 录好的演示视频兜底。
- **10 天不够** → 砍副功效/五味/相使相杀（已标"看工期"项），保核心三 wow 点。

## 待办
- [x] 知乎开放平台 Bearer Token 申请（额度已升：搜索5000/直答100/天）
- [x] 麻黄汤 4 味药 + 风寒表实证 BOSS 种子数据录入
- [x] 病证搜索主题池 10 条（风寒/风热/湿热/气虚/阴虚/血瘀/肝郁/痰湿/实热/阳虚）
- [x] 通用解药池扩功效覆盖（19 味，全 12 功效 + 相须/相恶/相反三对教学对）
- [x] 三层牌堆 + 心力费用（deck.ts 纯函数 + 5 case selftest）
- [x] 结算数值 playtest 调参（selftest 25 case：麻黄汤5场景+七情3+复杂可解1+牌堆5+难度闸+病机一致性）
